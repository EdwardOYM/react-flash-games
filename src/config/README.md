# Runtime Configuration

`default.config.json` defines the initial settings and highscore shape for the browser app. Runtime changes are persisted as a single `flash-games.config` record so the bundled default file remains immutable.
