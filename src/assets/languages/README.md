# Language Assets

Locale dictionaries for the game hub live here:

- `en.json` - English
- `ms.json` - Malay
- `zh.json` - Simplified Chinese

Keep keys aligned across all locale files. Import these JSON files from UI components when localization is wired into the app.
