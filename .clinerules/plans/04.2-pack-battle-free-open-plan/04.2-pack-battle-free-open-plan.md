# Implementation Plan — Pokémon Pack Battle: free-order own-pack opening (04.2)

> **Status: ACTIVE — scheduled by the user 2026-09-24.** Supersedes the linear
> v2 ceremony (`pair-open {pairIndex}` / `card-reveal {pairIndex, cardIndex}`,
> one round = one pack per seat, mirrored lockstep reveal). The 04.1 plan's
> "Deferred: protocol v3" note now points here as the live plan.

## Goal

Let each player open and reveal **their own packs**, in **any order**, with every action still mirrored to the
other screen — replacing the current linear "one round = one pack per seat, revealed in lockstep" cursor.
The reveal interaction mirrors the solo rip page (`PokemonPackRip`): each opened pack shows its cards
face-down, clicking a card reveals just that card, and a reveal-all button flips the rest at once.

## Required inputs (user — locked)

- Each player opens their **own** packs only, **in any order they want** (any pack, any card, any order).
- It **still shows on both screens**: if player 1 opens their pack 1, player 1's pack 1 opens on player 2's
  screen too; if player 2 opens pack 4, that pack 4 opens on player 1's screen too.
- Both seats keep playing simultaneously — no shared round cursor, no waiting for the other seat's turn.
- Card reveal follows `pokemon-pack-rip`: any card clickable in any order + reveal-all.

## Flow checks (02-diagram-flow-checks)

- **Persistence shape**: no `AppConfig` change. Session-only per-pack ceremony state
  (`opened: boolean`, `revealed: boolean[]` per pack index); persisted writes stay exactly as today —
  `recordOpenedCards` once per ceremony for the local seat's own packs (`cardIdsForSeat`), plus the solo
  rip path. Single storage key `flash-games.config` preserved.
- **State machine**: `opening` stays one view; completion still gates `summary` behind the click gate
  (`ceremonyComplete` = every pack opened AND every card revealed → `actionSeeAll` → `summary`).
  `summary` / `highscore` / rematch / leave edges unchanged.
- **Component flow**: game logic stays in `pokemon-pack-battle/`; P2P I/O still funnels through
  `net/peer.ts` sessions; persistence still only via `src/config/index.ts`.
- **Localization flow**: new/changed copy goes through `en/ms/zh.json` + `PackBattleTranslationKey` union
  first (01 + 02 localization flow), parity en==ms==zh before rendering it.

## Design constants

- Protocol **v3**: per-pack `pack-open {packIndex}` + `card-reveal {packIndex, cardIndex}` replace the
  round-based `pair-open` / `card-reveal {pairIndex, cardIndex}`; plus idempotent `ceremony-sync`
  `{ opened: number[], revealed: Record<number, number[]> }` (union-merge snapshot) for re-dial catch-up.
  Bump `PACK_BATTLE_PROTOCOL_VERSION` to 3 — the version guard already rejects mismatched builds,
  so v2↔v3 cross-play fails closed with `errorVersion`.
- State: `{pairIndex, cardIndex, opened}` cursor → per-pack map keyed by pack index:
  `packs: Record<packIndex, { opened: boolean, revealed: boolean[] }>` (6 flags, any order).
  `revealedAnim` cascade retires with the linear cursor (clicks flip instantly via `.pkm-card-flipper`;
  reveal-all flips the rest at once — same as the rip page, no cascade timer).
- Ownership: `seatForPack(packIndex, totalPacks)` gates **clicks** (host = even, guest = odd,
  odd tail `both` = either seat; first click wins, union-merged). **Rendering is ungated** — every
  opened/revealed pack shows on both screens.
- Scoring unchanged (identity scoring, Pikachu IR = 0); totals sum revealed flags per pack instead of the
  cursor prefix. Completion = every pack opened AND all 6 flags set on every pack.
- Layout: per-seat pack panels (host packs / guest packs, tail pack in both or its own row) with
  pagination or scroll — 36 packs = 18 per seat cannot render one round at a time. 960×540 embed,
  `min(100%, 960px)` caps, mobile-landscape `100dvh` pattern, ≤520px portrait stacking (per 01).
- Copy: `packProgress` ("Round {current} of {total}"), `packsLeft`, `ceremonyOpenHint`,
  `ceremonyRevealHint`, `actionRevealPack` ("Reveal both packs"), `actionNextPack`, `tutorialOpen` change
  meaning (no rounds, no lockstep, per-seat progress). Old round keys retired or reworded in the same
  locale pass — never leave a dead key in the dicts.
- The rip page (`PokemonPackRip`) is untouched — it is the reference UX, not a consumer.

## Progress

- [x] CP0 — Plan activated + breakdown (this edit): status ACTIVE, goal/inputs/design locked, checkpoints
  CP1–CP6 below. **Validate: no code; plan + diagrams referenced.**
- [x] CP1 — Protocol v3 (`net/protocol.ts`): version 3, `pack-open {packIndex}` + `card-reveal
  {packIndex, cardIndex}` + `ceremony-sync {opened, revealed}` types and guards; `validPackIndex` is bounded
  by `maxPacksPerPlayer * 2`. The still-linear CP1 consumer uses explicitly temporary `pair-open` /
  `card-reveal-pair` compatibility messages so the checkpoint remains buildable; CP2 removes them with
  the cursor. **Validate: build + lint — passed (0 warnings, 0 errors; pre-existing chunk-size advisory).**
- [ ] CP2 — Per-pack ceremony state (`PokemonPackBattleGame.tsx`, no layout yet): per-pack
  `{opened, revealed[]}` map, ownership-gated `openPack(packIndex)` / `revealCard(packIndex, cardIndex)` /
  `revealAllPack(packIndex)` senders + union-merge receivers, totals from revealed flags, `ceremonyComplete`
  = all packs opened + all flags set; re-hello replays `ceremony-sync` instead of pair/card cursor.
  Retire `cursor` / `revealedAnim` cascade / `nextPair` / round max-merge. **Validate: build + lint +
  logic harness (ownership, union-merge, totals, completion).**
- [ ] CP3 — Localization: new/changed `packBattle.*` progress + hint + reveal-all + panel copy in
  `en/ms/zh.json` + `PackBattleTranslationKey` union; retire dead round keys; rewrite `tutorialOpen`
  (any-order own packs, mirrored on both screens). Parity en==ms==zh. **Validate: build + lint +
  locale parity.**
- [ ] CP4 — Ceremony layout (`PokemonPackBattleGame.tsx` + `.css`): per-seat pack panels with pagination
  or scroll, per-pack sealed → face-down grid → any-order flip (rip mirror: `ppb-card-button` +
  `faceDown` + `revealCardLabel` aria), per-pack reveal-all, per-seat progress counters; co-located CSS
  only, 960×540 + landscape + ≤520px. **Validate: build + lint + viewport audit.**
- [ ] CP5 — Remappable keys + re-dial hardening: Confirm/Skip drive open/reveal on the focused pack
  (ownership-gated, same bindings `pokemon-pack-confirm` / `pokemon-pack-skip`); host `ceremony-sync`
  replay on re-hello; same-seed `lobby-start` no longer resets the per-pack map. **Validate: build +
  lint.**
- [ ] CP6 — Diagrams + final validation: `architecture-flow.md` (v3 messages, per-pack state, panel flow,
  sync replay) + `database-schema.md` prose (no schema change — session-only state confirmed);
  build + lint + locale parity + human QA (two-tab Cloud: any-order clicks mirror, totals match,
  completion gates summary, rematch reseeds).

## Hard rules

- Same as 04.1 and the project standards: translated copy first, co-located CSS per component, 960×540 embed,
  one checkpoint per turn with a reported gate, verify on disk before claiming.
