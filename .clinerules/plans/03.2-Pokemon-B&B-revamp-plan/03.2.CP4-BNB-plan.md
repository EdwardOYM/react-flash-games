# 03.2.CP4 — B&B Turn Phases, Actions, and Between-Turns

> Parent: `03.2-Pokemon-B&B-revamp-plan.md`. Depends on CP3.

## Scope

Implement the supplied `Draw → Main → Attack/Pass → Between-Turns` sequence. Draw resets limits,
draws one, and settles deck-out before Main. Main actions are any-order: Basic-to-Bench, evolution, Item,
Tool, and eligible during-turn Ability; once-per-turn Energy, Supporter, Stadium, and Retreat. Enforce
first-turn restrictions, same-name Stadium rejection, exact retreat payment/once-per-side limit, and
Asleep/Paralyzed attack/retreat blocks. Add explicit Pass as the non-attack end-turn action.

Rewrite Checkup as global Poison → Burn → Asleep → Paralysis resolution, then between-turn effects, then
all KOs/prizes. Replace single promotion state with an ordered queue for simultaneous KOs, next player first.

## Files

Engine types/actions/turns/effects/helpers, action controls, locales.

## Validation

Draw/deck-out, out-of-phase rejection, any-order Main actions, every limit, first-turn restrictions,
Retreat/Pass, ordered status trace, simultaneous KO promotion, snapshots, build + lint.

## Progress

- [x] Explicit `draw → main → attack → between` phases: Draw resets limits/draws once, resolves deck-out, then Main begins.
- [x] Main accepts any-order Basic-to-Bench, evolution, Item, Tool, eligible during-turn Ability, Energy, Supporter, Stadium, and Retreat actions through typed intents.
- [x] Enforced Bench capacity, Tool capacity, same-name Stadium rejection, one-per-turn Energy/Supporter/Stadium/Retreat/Ability, first-player Turn-1 Supporter/Attack restrictions, exact retreat payment, and Asleep/Paralyzed attack/Retreat blocks.
- [x] Explicit `beginAttack → useAttack/pass` closes the final action; stale End Turn intent is removed from engine/UI.
- [x] Between-Turns resolves all Poison, then all Burn, all Asleep, all Paralysis, then queued card-effect seam, then next-player-first KO/Prize order.
- [x] Simultaneous KOs use an ordered promotion queue; promotions remain the only legal actions until both sides are promoted, then the next Draw Phase begins.
- [x] Shared Stadium and attached Tool/Turn state are preserved in snapshots; hidden-zone privacy and view-only authority remain intact.
- [x] UI displays the active phase and separates Main controls from Attack/Pass; all labels/errors/logs are translated in en/ms/zh.
- [x] Validation passed: Draw and deck-out; Main/Attack out-of-phase rejection; any-order Basic/Item/Tool/Energy/Supporter/Stadium/Ability; every once-per-turn/first-turn/same-name/Tool/Bench/Retreat limit; Retreat cost; Pass; ordered Poison→Burn→Asleep→Paralysis trace; simultaneous-KO next-player-first queue and two promotions; snapshot view-only authority; 601-key locale parity; `npm run build`; `npm run lint`; `git diff --check`.

Completed 2026-09-25. Temporary assertion artifacts removed. No persistence schema changed; diagram synchronization remains scheduled for CP10.
